import 'src/bootstrap_spike_request.dart';
import 'src/bootstrap_spike_result.dart';

class FastBootstrapSpikeRunner {
  Future<FastBootstrapSpikeResult> run(FastBootstrapSpikeRequest request) {
    throw UnimplementedError(
      'Bootstrap spike scaffold only. Generator and child runner are added in later commits.',
    );
  }
}

