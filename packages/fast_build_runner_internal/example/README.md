# fast_build_runner_internal example

This package exposes internal runtime pieces used by
`fast_build_runner`.

Minimal example:

```bash
dart run example/main.dart
```

It prints the currently pinned upstream `build_runner` commit and the hosted
constraint used by the runtime package.
