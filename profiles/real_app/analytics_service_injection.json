{
  "name": "real_app analytics service injection",
  "sourceFileRelativePath": "lib/core/injection/register_module.dart",
  "generatedFileRelativePath": "lib/core/injection/injection.config.dart",
  "steps": [
    {
      "name": "add analytics endpoint provider",
      "generatedMarkers": [
        "provideAnalyticsEndpoint()"
      ],
      "replacements": [
        {
          "from": "  /// App logger (DI)\n  @lazySingleton\n  AppLogger provideAppLogger() => const AppLogger();\n}\n",
          "to": "  /// App logger (DI)\n  @lazySingleton\n  AppLogger provideAppLogger() => const AppLogger();\n\n  /// Analytics endpoint label for diagnostics.\n  @Named('analytics_endpoint')\n  String provideAnalyticsEndpoint() => 'analytics';\n}\n"
        }
      ]
    }
  ]
}
