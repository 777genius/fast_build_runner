name: fast_build_runner
description: Faster watch and incremental rebuild workflows for build_runner-based Dart and Flutter projects.
version: 0.1.0
repository: https://github.com/777genius/fast_build_runner
homepage: https://github.com/777genius/fast_build_runner
environment:
  sdk: ^3.10.0

executables:
  fast_build_runner: fast_build_runner

dependencies:
  args: ^2.7.0
  fast_build_runner_internal: ^0.1.0

dev_dependencies:
  build: any
  lints: ^6.0.0
  path: ^1.9.1
  test: ^1.25.5
  watcher: ^1.2.1
